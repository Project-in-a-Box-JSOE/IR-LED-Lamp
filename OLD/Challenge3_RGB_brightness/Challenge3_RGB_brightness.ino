#include <Arduino.h>

const int redPin = 4;
const int greenPin = 5;
const int bluePin = 6;

int brightness = 255;

void setup() {
    pinMode(redPin, OUTPUT);
    pinMode(greenPin, OUTPUT);
    pinMode(bluePin, OUTPUT);
}

void loop() {

}